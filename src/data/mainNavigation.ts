import { paths } from '@/data/paths';

export const mainNavigation = {
  Marketplace: {
    'Home': '/',
    'NFT Listings': paths.nftListings,
    'Venues': paths.venues,
    'Create Avatar': paths.createAvatar,
    'Creator': paths.creator,
    'FAQ': paths.faq,
  },
};
